{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Docker: Attach to Delve",
        "type": "go",
        "request": "attach",
        "mode": "remote",
        "remotePath": "/app", // Путь внутри контейнера
        "port": 40000,        // Порт Delve
        "host": "127.0.0.1",  // Хост машины
        "program": "${workspaceFolder}", // Ваш код
        "apiVersion": 2
      }
    ]
  }